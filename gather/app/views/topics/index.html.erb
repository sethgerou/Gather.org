<div class="topics_list two">
  <h1>TOPICS</h1>
  <% if current_user %>
  <%= button_to "add a topic", new_topic_path, method: "get" %>
    <% end %>
  <% @topics.each do |topic| %>
    <h3 class ="topic"><%=link_to topic.name.capitalize, topic_path(topic.id) %></h3>
  <% end %>
</div>
